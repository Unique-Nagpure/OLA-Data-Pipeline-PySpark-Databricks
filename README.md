# OLA-Data-Pipeline-PySpark-Databricks
End-to-end OLA ride-hailing data pipeline using PySpark and SQL on Databricks — ingests CSV/JSON/Parquet from ADLS, runs explicit data quality checks and validation, transforms with medallion architecture (bronze/silver/gold), and writes to Delta tables.
